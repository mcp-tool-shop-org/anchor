[package]
name = "anchor-core"
version = "0.1.0"
edition = "2021"
description = "Anchor drift-prevention engine — Rust backend"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
